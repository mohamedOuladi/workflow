import { DynamicPluginDirective } from './dynamic-plugin.directive';

describe('DynamicPluginDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPluginDirective();
    expect(directive).toBeTruthy();
  });
});
